
Como ya tengo todo intalado  y creado lo primero es hacer correr airflow

creamos la variable de entorno AIRFLOW_HOME
export AIRFLOW_HOME=/home/tackel/apache-airflow-aceleracion/airflow

luego levantamos el web server:
airflow webserver -p 8008 # o el puerto que no este ocupado


luego hacer correr el scheduler en otra terminal:
instaciar la variable:
export AIRFLOW_HOME=/home/tackel/apache-airflow-aceleracion/airflow
luego:
airflow scheduler


crear propios dags:
navegar hasta donde este intalado airflow
cd apache-airflow-aceleracion/airflow
Abrir code con: code .